const Events = () => {
    return (
      <div>
        <h1>Upcoming Events</h1>
        <p>Find all the latest events here.</p>
      </div>
    );
  };
  
  export default Events;  // ✅ Make sure this line is present
  